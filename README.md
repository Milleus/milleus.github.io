# Milleus.github.io
Milleus.github.io is my personal website running on Github, using Gitpages with Jekyll.

## How to run locally?
1. Install Ruby.
2. Install gem packages with `bundle install`.
3. Go to folder in terminal and run command `bundle exec jekyll serve`.
4. View website at `http://127.0.0.1:4000`.
